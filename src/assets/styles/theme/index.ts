import { primary } from '@/src/assets/styles/theme/_primary';
import { secondary } from '@/src/assets/styles/theme/_secondary';
import { dark } from './_dark';

const theme = {
  primary,
  secondary,
  dark
};

export { theme };
