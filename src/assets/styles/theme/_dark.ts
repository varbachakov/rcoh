import { typography } from '../base';
import { colors } from '../variables';

const dark = {
  family: typography.fontFamily.one,
  colors: {
    text: colors.white.one,
    backgroundColor: colors.dark.one,
  }
};

export { dark };
