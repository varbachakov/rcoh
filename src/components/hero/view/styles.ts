import css from 'styled-jsx/css';

const styles = css`
  .hero-view {
    position: relative;
    margin: auto;
  }

  .hero-view__canvas {
    width: auto;
    height: auto;
  }

  .hero-view__img {
    position: absolute;
    top: 0;
  }
`;

export default styles;
