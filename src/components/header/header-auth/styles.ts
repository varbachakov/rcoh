import css from 'styled-jsx/css';

const styles = css`
  .auth {
    position: relative;
    width: 90px;
  }
  
  .auth__button {
    display: flex;
    align-items: center;
  }
  
  .auth__name {
    width: 50px;
    text-align: center;
  }
  
  .auth__img {
    width: 40px;
    height: 40px;
    border-radius: 50%;
    overflow: hidden;
  }
`;

export default styles;
